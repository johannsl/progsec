{% extends 'base.twig' %}

{% block content %}

<h2>User</h2>

{{ flash.info }} <br />
{{ flash.error }} <br />

{% if user %}
    <table>
        <tr>
            <th>Id</th>
            <th>{{ user.userid }}</th>  {#  The variable shouldn't be escaped  #}
            <tr></tr>
            <th>Username:</th>
            <td>{{ user.username }}</th>  {#  The variable shouldn't be escaped  #}
            <tr></tr>
            <th>Bio:</th>
            <td>{{ user.bio }}</td>  {#  The variable shouldn't be escaped  #}
            <tr></tr>
            <th>Age:</th>
            <td>{{ user.age }}</td>  {#  The variable shouldn't be escaped  #}
       }     <tr></tr>
        <!-- PS: RMB4P!!! {{ user.hash|raw }} --></td> {# User hash shouldn be in page comments (G21_0005) #}




    </table>
{% else %}
    Could not find a user with username "{{ username }}".  
{% endif %}

{% endblock %}
